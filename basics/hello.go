package main

import (
	"fmt" // fmt stands for format (standard library package
)

func Hello() {
	s := "gopher"
	fmt.Printf("hello and welcome, %s!\n", s) //  this is formating print which uses formating verbs
}
